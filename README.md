# MongoDBConnection

gradle:

```text
repositories {
    maven { url 'http://framedev.stream:82/releases' }
}
dependencies {
    compileOnly 'de.framedev:MongoDBConnection:1.0-SNAPSHOT'
}
```
Maven:
```text
<repository>
  <id>repository</id>
  <url>http://framedev.stream:82/releases</url>
</repository>
<dependency>
  <groupId>de.framedev</groupId>
  <artifactId>MongoDBConnection</artifactId>
  <version>1.0-SNAPSHOT</version>
</dependency>
```
